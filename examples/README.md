# Examples

`sample.log` contains synthetic data only.

Run:

    python parser.py --file examples/sample.log --field url

or:

    python parser.py --file examples/sample.log --field user

or:

    python parser.py --file examples/sample.log --field both

Do not place real credentials or other secrets in this directory.
